apiVersion: v1
kind: Service
metadata:
  name: {{ include "solar-system-app.fullname" . }}
  labels:
    {{- include "solar-system-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    {{- include "solar-system-app.selectorLabels" . | nindent 4 }}
